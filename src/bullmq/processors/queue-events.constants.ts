export enum QueueEvents {
  DEFAULT_QUEUE = 'default',
  NOTIFICATION_QUEUE = 'notification',
  AUDIT_LOGGER_QUEUE = 'audit-logger',
  EMAIL_QUEUE = 'email',
  ALERT_QUEUE = 'alert',
  COMPLAINT_ESCALATION_QUEUE = 'complaint-escalation',
}
